[project]
name = "asteriods"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jaeri022", email = "jaeridley@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "pygame==2.6.1",
]

[project.scripts]
asteriods = "asteriods:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
